import 'package:flutter/material.dart';

class AcceptedApplicationsScreen extends StatelessWidget {
  const AcceptedApplicationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}